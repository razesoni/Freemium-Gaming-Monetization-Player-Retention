# Contributing

Use a feature branch, explain the analysis or bug being addressed, and run `python -m pytest tests/`. For metric changes, document the population, time window, denominator and treatment of missing values. Include a small regression example. Never commit API keys or private player data.
